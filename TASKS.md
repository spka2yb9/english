# TASKS — CEFR Reach B2 English Learning Platform

実装と検証の進行記録。2026-09-01 時点。

## Phase 1–6: 設計とデータモデル

- [x] Vite + React + TypeScript、React Router、Vitest、Testing Library、jsPDF の構成
- [x] 学習設計仕様 (`docs/learning-design.md`)
- [x] Reach B2 全文法カリキュラム (`docs/grammar-curriculum.md`)
- [x] 語彙選定方法 (`docs/vocabulary-methodology.md`)
- [x] 主要参照資料 (`SOURCES.md`)
- [x] 文法／問題／図解／語彙／発音／進捗の型 (`src/content/types.ts`)

## Phase 7–9: 音声、シェル、発音

- [x] `SpeechService` 抽象と Web Speech API アダプター (`en-US`)
- [x] 通常／低速再生、再生置換、キャンセル完了、非対応時フォールバック
- [x] `AudioButton` / `EnglishExample` とARIAラベル、キーボード操作
- [x] Home／発音／文法／語彙のルーティングとルート単位コード分割
- [x] デスクトップサイドバー、モバイルドロワー、Escape、スキップリンク
- [x] ダッシュボード、文法進捗、語彙統計、localStorage障害時フォールバック
- [x] 39 IPA項目、13最小対、schwa／強勢／弱形／連結／語末子音

## Phase 10–12: 文法エンジン、出力、教材

- [x] 目標 → 解説 → 図解 → 例文 → 対比 → クイズ → まとめの共通UI
- [x] タイムライン24、構造図36、対比67、表70のデータ駆動レンダリング
- [x] 即時正誤、正解理由、誤答理由、正解英文音声を持つクイズ
- [x] 個別／ユニット／全体の Markdown生成
- [x] IPAexゴシックを使った日本語PDF生成と遅延ロード
- [x] A2 U01–U06: 23セクション
- [x] A2+ U07–U10: 16セクション
- [x] B1 U11–U16: 25セクション
- [x] B1+ U17–U22: 19セクション
- [x] B2 U23–U31: 27セクション
- [x] 合計110セクション、440問、全問題に正解音声

## Phase 13–16: 語彙

- [x] Fisher–Yatesによる重複なし10語選択
- [x] 意味を隠した「わかる／わからない」想起フロー
- [x] わからない語だけを0語になるまで再出題する純粋状態機械
- [x] 単語音声／例文音声、意味、品詞、IPA、コロケーション、関連語、任意の語形成
- [x] Oxford A2–B2差分1,950語と高頻度句動詞2語を追加
- [x] A2 657 + B1 984 + B2 1,359 = 3,000見出し語
- [x] 拡張語彙の独自例文1,180件、出典表示付き対訳770件
- [x] 正誤率と最終出題日による説明可能な復習優先度
- [x] 重複、欠損、発音、TTS用例文、見出し語使用の自動検証
- [x] 全3,000語に語源・コアイメージ中心の日本語解説文(`notes.ts` → `mnemonic`)
- [x] 「覚えるヒント」を `<details open>` で常時展開

## Phase 17–19: QA

- [x] 語彙選択／再試行／進捗／音声／クイズ／ナビ／Markdownの自動テスト
- [x] 全110文法セクションのID、前提順、15分上限、問題・音声・まとめの検証
- [x] 全3,000語の一意性、必須項目、例文形式の検証
- [x] 3,000語拡張後の最終 `test` / `lint` / `build`（67テスト・コンテンツ検証16項目成功）
- [x] 375 / 768 / 1024 / 1440px のスクリーンショットQA
- [x] 全110セクションPDFの実ブラウザ生成（279ページ）
- [x] README・件数・最終監査結果の確定

## Phase 20: 品質監査の反映

- [x] クイズ選択肢を表示時にシャッフル(正解位置がA/Bに82.5%偏っていた問題の修正)
- [x] ホームを生成サマリー参照に変更し、初回読み込みから教材本体 gzip 約700KB を除去
- [x] 語彙の復習を予定日ベースの間隔反復へ(連続正解で間隔倍増、誤答で翌日へリセット)
- [x] `npm run summary` とサマリー整合性テストの追加

## Phase 21: 演習量とインプットの拡充

- [x] can-do カバレッジを `npm run summary` で機械集計
- [x] CEFR can-do 記述子(読む・聞く・文法・語彙)と、全ユニット・全コンテンツへのマッピング
- [x] 生成コンテンツ検証ハーネス(`content/validation.ts` + `npm run validate:batch`)
- [x] 文バンク(既存4,098文の集約)と、実行時に問題を組み立てる派生生成
- [x] 演習5モード: 音読／ディクテーション／和文英訳／並べ替え／文法SRS復習
- [x] 入力採点(短縮形・句読点・大小文字の吸収、LCSによる語単位差分)
- [x] 多読18本(A2/B1/B2 各6本、設問90問、語注・段落和訳・段落音声つき)と多読UI
- [x] 学習量ページ・判定テスト・ライティングは方針変更により削除
- [x] 多読の増量: A2・B1・B2 を各36本(計108本・設問540問)へ拡充
      (`passages-b2b.ts` / `passages-a2c.ts` / `passages-b1c.ts` / `passages-b2c.ts`)
- [x] 多読の読了を自己申告に変更(クイズ完了での自動記録を廃止し、文法と同じ完了ボタンへ)。
      一覧はレベルごとの `<details>` で折りたたみ、最初の未読了レベルだけ開く
- [ ] 文法演習を2,200問へ(6形式)
- [ ] 事前生成音声への移行(判断基準: ディクテーション完了率が他モードより20pt低い場合)

## Phase 22: B2到達性の監査反映

監査で「演習量が1桁不足」「B2固有項目に欠落」「B1+→B2で難度が上がっていない」「B2でL1対比が薄い」
と判定された4点への対応。

- [x] クイズ数をレベル別ゲート化(`REQUIRED_QUIZ_COUNT`。B2=10問、他=4問)
- [x] 既存B2 27セクションを4問→10問へ増量(+162問)
- [x] B2例文にレッスン平均語数の下限を導入(`MIN_AVG_EXAMPLE_WORDS` B2=12語)。
      多読と同じくレベル逸脱を機械検査する。既存B2例文を書き換えて平均9.2語→13.1語
- [x] U32「時制と条件の総仕上げ」4セクション新設
      (未来進行形+未来完了 / 過去から見た未来 / 倒置による条件文 / the 比較級構文)
- [x] U33「日本語話者が最後まで残す誤り」3セクション新設(冠詞 / 数量詞 / 句動詞の文法)
- [x] B2の19セクションの補強解説に「日本語ではこう考える」段落を追加。
      L1対比のあるレッスンは36/110 → 62/117
- [x] `Not only ...` の例文と問題を `but ... also` を伴う標準形に修正
- [x] 文法ページでクイズ結果を記録する(`GrammarLesson.tsx` に `onAnswer` を渡す)。
      `recordGrammarItemAnswer` は `practice.ts` から `progress.ts` へ移設した — `practice.ts` は
      文バンク(語彙・多読を含む)を import しており、文法ルートのチャンクに持ち込まれるため。
      回帰テストを `GrammarLesson.test.tsx` に追加
- [ ] 誤答のみ再挑戦する導線を `Quiz.tsx` に追加する
- [ ] 各セクションに和文英訳を1問置き、産出練習を入れる

## Phase 22: 語彙を4,500見出し語へ

B2到達の目安とされる語彙サイズ(4,000〜5,000語族)に合わせ、3,000語から1,500語を増補した。

- [x] 増補1,500語の選定: Oxford 5000 C1帯の未収録1,267語 + 高頻度句動詞145件 + 定型表現31件 +
      派生語・日常語57件
- [x] 全1,500語の語義・例文・和訳・コロケーション・覚えるヒントを自作(外部の定義文・例文は不使用)
- [x] CMUdict からのIPA生成、英綴り語の米綴り引き当て
- [x] 作業ツール `scripts/vocab4500.mjs`(`prepare` / `status` / `check` / `build`)と、
      取り込み前に全件をまとめて落とす `check`(語数・禁止記号・見出し語の使用・解説文の長さ)
- [x] `set up` と `set-up` の id 衝突を機械的に排除
- [x] 既存1,953語へコロケーションを後付けし、全4,500語がコロケーションを持つ状態にした
      (`collocations.ts` を `notes.ts` と同じ方式で合流。既存データは書き換えない)
- [x] A2 695 + B1 1,079 + B2 2,726 = 4,500見出し語。`summary` 再生成、`test` / `lint` / `build` 成功

## Phase 23: 図解の全セクション化と、演習の解体

演習ページは「学んだ英文を別の形で使い直す」棚だったが、産出練習は学んだ直後のセクション内にある
ほうが効く。並べ替えを文法セクションへ移し、残った音のみのモードでページを組み直した。

- [x] 図解のない17セクションに構造図・対比図・タイムラインを追加。全117セクションが図を持つ
      (`content.test.ts` の視覚ブロック検査から `table` を外し、退行を検出できるようにした)
- [x] 並べ替えを文法セクション末尾へ移設(`components/WordOrder.tsx`)。出題はそのセクション自身の
      例文10文(計1,170問)。文バンクを経由しないので文法ルートのチャンクは増えない
- [x] 全117セクションの例文を10文へそろえた(202文を追加。`expansions/extraExamples.ts` に合流させ、
      既存の教材ファイルは書き換えない)。3語以下だった4文は並べ替えに使えるよう自然な長さへ伸ばした
- [x] `content.test.ts` で「例文はちょうど10文・重複なし・各4語以上」を固定。例文10文が並べ替え10問の母集団
- [x] 演習ページから和文英訳・文法SRS復習・並べ替えを削除。穴埋め生成(`buildClozeItem`)も破棄
- [x] 残る音読・シャドーイングとディクテーションに合わせ、ページ名を「音声練習」へ変更。
      モードが2つになったため一覧画面を廃し、上部トグルで切り替える形へ
- [ ] 語彙・多読にも同じ考えで産出練習を置くか検討する

## Phase 24: 5文型を全セクションへ

文型の視点を教材全体の共通言語にする。例文ごとに5文型を付けるため、判定規約を定めて機械検証可能にした。

- [x] U01 に 5文型のセクション `u01-l5` を新設
- [x] 全118セクションに「文型の視点」ブロックを合成時に差し込む(`patterns/` の focus。全セクション必須で検証)
- [x] 例文・対比の英文1,498件に5文型バッジと文型注記を付与。教材ファイルは書き換えず合成時に付与
- [x] 判定規約(疑問文・命令文・受動態・there is・名詞節・副詞句・関係詞節の扱い)を `patterns/types.ts` に明文化
- [x] `content.test.ts` で「全例文に文型と注記が付く」ことを固定。`scripts/grammar-patterns.mjs` で作業・点検
- [x] バッジと注記を UI・Markdown・PDF の同じデータから出力(`EnglishExample` / `export/`)

## Phase 25: 英文の作られ方を1ページへ統合する

文法事項を個別に覚えるのではなく、英文の骨格(S / V / O / C)と修飾語(M)を自分で見つけられるようにする。

- [x] 英文法ページからリンクで進む「英文の作られ方から学ぶ」ページ(`/grammar/roadmap`)を新設
- [x] ページの冒頭で S / V / O / C / M が何を表すかを、役割バッジ付きのカードと構造図で説明
- [x] 旧 `u01-l4`「英語の語順 SVO」と旧 `u01-l5`「5文型で文の骨組みを見る」をページへ統合し、
      カリキュラムから削除(118→116セクション、676→668問、例文1,180→1,160文)
- [x] 統合で不足する内容を補完(記号の意味を問う問題、代表例の文型を問う問題、語順の組み立てアニメーション)
- [x] 5文型すべてに2Dアニメーションを追加。形式はレッスンの挿絵と同じ `scene(title, mode, frames)` とし、
      再生UI(`GrammarAnimation`)を挿絵と共用
- [x] 理解度チェック23問をページに置き、正誤を文法SRSへ記録(`roadmap-*` のIDで既存クイズと衝突しない)
- [x] `content.test.ts` でページの内容(記号の網羅・ブロックの構造検査・全5文型のアニメーション・問題の構造検査)を固定
- [x] Markdown/PDFはレッスン定義から生成する経路のまま(ページは導線と解説なので対象外)

## Phase 26: 音声練習を終わりのない周回出題へ

8問セッションで区切ると、そこで練習が止まり、続けたいときは「次の8問へ」を押して区切り直すことになる。
音読・シャドーイングとディクテーションは「学んだ英文を何度も声に出し、書き取る」反復の場なので、
問題数と終わりを外した。

- [x] `PRACTICE_SIZE` とセッション一括抽選(`selectPracticeSentences`)を廃止し、1文ずつ引く
      `drawPracticeSentence` へ置き換え。完了画面(「次の8問へ」)も削除
- [x] 記録するのは文ごとの実施回数だけ(`eng.practice.stats.<mode>` に `Record<sentenceId, number>`)。
      実施回数の少ない文から順に全体を1周ずつ回し、同数の文の中ではランダムに選ぶ。
      旧形式(WordStatオブジェクト)の履歴は、読み込み時に `seen` を実施回数として引き継ぐ
- [x] 周回の位置を別に持たないので、実施回数さえGistで同期すれば端末をまたいでも続きの文から回れる
      (正誤は記録しない。目的が「今復習すべき文」を当てることではないので、間隔反復には乗せない)
- [x] 進捗は「n問目」と、ディクテーションの正解数だけを表示
- [x] `practice.test.ts` / `Practice.test.tsx` / `Settings.test.tsx` を出題方式に合わせて更新
      (test / lint / build 成功)

## Architectural decisions

- 教材は `src/content/` のTypeScriptオブジェクトとしてUIから分離。同じ文法データからWeb／Markdown／PDFを生成。
- 音声実装は共有サービスへ隔離し、将来クラウドTTSや事前生成音声へ交換可能。
- PDFライブラリと各学習領域は動的importし、初期バンドルを分割。
- 語彙セッションと復習選択はReactから独立した純粋関数として検証。
- 既存データが消去・破損・保存不可でも、教材閲覧と新しいセッションは継続可能。
- ホームは教材データを import しない。件数と次のセクションは生成ファイル `src/content/summary.ts` から取る。
- 演習問題はデータとして持たず、既存の英文から実行時に生成する。演習量を増やしてもバンドルは増えない。
- SRSは語彙と文法問題で同じ `vocabSelection` を使う。対象がIDで表せるならスケジューラは1つでよい。
  音声練習の文は実施回数だけを記録し、少ない文から順に1周ずつ回す別の仕組みにする
  (同期するのは回数だけなので、周回のカーソルを持たずに端末をまたげる)。
- 語彙の増補分は行タプル形式の生成ファイル(`extended.ts`)に置き、`mnemonic` も同ファイルに持つ。
  `notes.ts` は既存3,000語の解説文専用のままにし、生成物どうしを混ぜない。
- 教師レビューを行わないため、品質ゲートは機械検証に集約する(`content/validation.ts` をCIと取り込み前検査で共用)。
- 文型は教材ファイルに書かず、`src/content/grammar/patterns/` に英文をキーとして持ち、合成時に付与する。
  補強解説・挿絵と同じ方式で、セクション数・問題数・例文数の集計を変えずに全レッスンへ通せる。
- レッスンではない解説ページは `src/content/grammar/structures/` に置き、lesson を持たない型(`LessonBlock[]` と
  `QuizQuestion[]`)で持つ。合成時の文型付与・挿絵差し込みを通らないため、文型バッジはデータ側で明示する。
- 2Dアニメーションは `scene()` の形式と再生UIを1つに保つ。レッスンの挿絵と文型の図解で実装を分けない。
